// const express = require('express');
// const router = express.Router();
// const controller = require('../controllers/postInteraction.controller');
// const { verifyTokenMiddleware } = require('../middlewares/auth.middleware');

// router.post('/:postId/comment', verifyTokenMiddleware, controller.addComment);
// router.post('/:postId/like', verifyTokenMiddleware, controller.toggleLike);

// module.exports = router;